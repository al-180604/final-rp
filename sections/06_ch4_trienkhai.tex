% ========================================
% CHƯƠNG 4: TRIỂN KHAI HỆ THỐNG
% ========================================
\chapter{TRIỂN KHAI HỆ THỐNG}
\label{ch:trienkhai}

% ----- TÓM TẮT CHƯƠNG -----
\vspace{0.5cm}
\noindent\textit{Chương này trình bày chi tiết quá trình hiện thực hóa các thiết kế hệ thống từ Chương 3 thành một mô hình prototype cụ thể trong phạm vi đề tài đồ án tốt nghiệp. Quá trình triển khai được trình bày theo định hướng kết hợp giữa học tập nghiên cứu giao thức mạng Zigbee và định hình giải pháp sản phẩm IoT (Hybrid "Learning to Product"). Nội dung chương đi sâu mô tả cấu trúc mã nguồn, cấu hình phần cứng, các module phần mềm của Gateway Edge nhúng, Cloud Backend và ứng dụng di động Mobile App, đồng thời vạch rõ các ranh giới kỹ thuật hiện tại.}
\vspace{0.5cm}

% ========================================
% 4.1 ĐỊNH HƯỚNG TRIỂN KHAI
% ========================================
\section{Định hướng triển khai: từ học Zigbee đến prototype hướng product}
\label{sec:ch4-orientation}

Quá trình triển khai hệ thống trong đề tài đồ án tốt nghiệp này không chỉ dừng lại ở việc lập trình các tính năng riêng lẻ hay cấu hình một mạng Zigbee cục bộ khép kín. Trọng tâm của chương này là mô tả cách hiện thực hóa bản thiết kế kiến trúc đa lớp đã đặt ra ở Chương 3, kết nối các thành phần từ tầng thiết bị vật lý, Gateway biên dịch giao thức, Cloud orchestration xử lý logic tập trung và ứng dụng di động Flutter. Việc tích hợp các lớp này nhằm tạo nên một giải pháp có khả năng vận hành đồng bộ và giải quyết bài toán quản lý thiết bị thông minh trong các tòa nhà (Smart Building).

Định hướng triển khai của đồ án tuân theo mô hình kết hợp Hybrid "Learning to Product". Mô hình này chia quá trình xây dựng hệ thống thành hai khía cạnh song hành:
\begin{itemize}
    \item \textbf{Khía cạnh học tập (Learning):} Giúp nghiên cứu sâu về các cơ chế cơ bản của giao thức Zigbee bao gồm cách Coordinator tạo lập mạng, cơ chế tham gia mạng (join/commissioning) của các Router, các tập lệnh chuẩn của thư viện Zigbee Cluster Library (ZCL), và việc truyền nhận trạng thái (attribute report) hay sự kiện (event) ở mức mạng cục bộ.
    \item \textbf{Khía cạnh sản phẩm (Product):} Định hướng thiết kế và đóng gói hệ thống tiệm cận với cấu trúc của một sản phẩm thương mại. Điều này được thể hiện qua việc tối ưu hóa hiệu năng Gateway (viết bằng ngôn ngữ native C giúp tiết kiệm tài nguyên hệ thống nhúng), tổ chức Cloud Backend theo mô hình REST API chuẩn hóa kèm theo hàng đợi xử lý lệnh không đồng bộ qua MQTT broker, quản lý trạng thái và dữ liệu sự kiện qua hệ quản trị cơ sở dữ liệu PostgreSQL, cung cấp giao diện quản trị trực quan trên Mobile App và tự động hóa quy trình triển khai bằng công cụ Docker Compose trên nền tảng điện toán đám mây.
\end{itemize}

Thông qua việc phân định rõ hai trục này, hệ thống vừa đóng vai trò như một môi trường thực nghiệm nghiên cứu mạng không dây công suất thấp, vừa phác thảo nên một khung giải pháp có khả năng mở rộng (scale-up) và sẵn sàng nâng cấp lên các tính năng thương mại hóa.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\scriptsize, >=Stealth]
        \tikzset{lbl/.style={font=\fontsize{6.5}{7.5}\selectfont, fill=white, inner sep=1.5pt}}
        \node[fblk, cCloud, font=\scriptsize, minimum width=3.6cm, minimum height=1.35cm] (local) at (0,0) {\textbf{Mạng Zigbee Cục bộ}\\\textit{(Lớp học tập)}\\Coordinator (NCP), Routers,\\Thuộc tính \& lệnh ZCL};
        \node[fblk, cGw, font=\scriptsize, minimum width=3.4cm, minimum height=1.35cm] (edge) at (5.4,0) {\textbf{Gateway Edge (Native C)}\\\textit{(Lớp biên dịch)}\\Cầu nối MQTT--Zigbee,\\Hàng đợi lệnh \& Registry};
        \node[fblk, cMob, font=\scriptsize, minimum width=3.6cm, minimum height=1.35cm] (cloud) at (10.8,0) {\textbf{Cloud \& Mobile UX}\\\textit{(Lớp sản phẩm)}\\FastAPI, PostgreSQL DB,\\REST API, Giao diện Flutter};
        \draw[farr] (local) -- node[lbl, above] {EZSP / ASH} (edge);
        \draw[farr] (edge) -- node[lbl, above] {MQTT Telemetry} (cloud);
        \draw[farr] (cloud.south) -- ++(0,-0.6) -| (edge.south);
        \node[lbl, below] at (8.1,-1.3) {Lệnh REST / MQTT};
    \end{tikzpicture}
    \caption{Định hướng triển khai tích hợp giữa nghiên cứu mạng Zigbee và giải pháp sản phẩm IoT.}
    \label{fig:ch4-learning-to-product}
\end{figure}

Hình \ref{fig:ch4-learning-to-product} minh họa rõ nét ranh giới và cách liên kết giữa các lớp. Mạng Zigbee cục bộ chịu trách nhiệm thu thập trạng thái vật lý và thực thi lệnh phần cứng. Gateway đóng vai trò trung gian biên dịch giao thức từ các gói tin nhị phân Zigbee sang dữ liệu JSON có cấu trúc qua MQTT broker. Lớp trên cùng bao gồm Cloud Backend và Mobile App cung cấp các giao diện lập trình ứng dụng (API) và trải nghiệm sử dụng (UX) cho người dùng cuối.

% ========================================
% 4.2 MÔI TRƯỜNG VÀ CẤU TRÚC MÃ NGUỒN
% ========================================
\section{Môi trường triển khai và cấu trúc mã nguồn}
\label{sec:ch4-implementation-env}

Hệ thống được tổ chức thành các thư mục mã nguồn riêng biệt, phân tách rõ ràng trách nhiệm của từng thành phần trong giải pháp đa lớp. Môi trường triển khai thực tế sử dụng các công nghệ hiện đại, phù hợp với năng lực xử lý của từng thiết bị. Bảng \ref{tab:ch4-source-map} tóm tắt các thành phần chính trong mã nguồn cùng công nghệ và vai trò tương ứng của chúng.
q
\begin{table}[H]
\centering
\caption{Môi trường công nghệ triển khai các thành phần hệ thống}
\label{tab:ch4-source-map}
{\footnotesize
\begin{tabular}{|p{4.5cm}|p{10.0cm}|}
\hline
\textbf{Thành phần} & \textbf{Công nghệ / Vai trò} \\ \hline
Firmware Zigbee & Silicon Labs Gecko SDK v4.5.0, EmberZNet, nạp qua Simplicity Commander \\ \hline
Gateway Edge & Native C, libmosquitto, EZSP/ASH qua kết nối serial UART \\ \hline
Cloud Backend & Python, FastAPI 0.115.6, SQLAlchemy async, Paho MQTT \\ \hline
Database & PostgreSQL 16, quản lý cấu trúc dạng quan hệ và các trường JSON mở rộng \\ \hline
Mobile App & Flutter SDK, Dart, quản lý trạng thái bằng thư viện Provider \\ \hline
Deployment & Docker Compose, shell script chạy trên máy chủ AWS EC2 Ubuntu 22.04 LTS \\ \hline
\end{tabular}
}
\end{table}

Việc tổ chức mã nguồn như trên giúp nhóm phát triển có thể độc lập xây dựng và kiểm thử từng module. Ví dụ, Cloud Backend có thể được kiểm thử bằng các kịch bản mô phỏng (mocking) thông qua \texttt{pytest} mà không cần có thiết bị vật lý hay Gateway chạy thực tế. Tương tự, ứng dụng di động Flutter có thể chạy ở chế độ giả lập dữ liệu bằng cách thay đổi cấu hình truyền dẫn sang API giả lập để phục vụ kiểm thử giao diện một cách nhanh chóng.

% ========================================
% 4.3 TRIỂN KHAI MẠNG ZIGBEE VÀ FIRMWARE
% ========================================
\section{Triển khai mạng Zigbee và firmware thiết bị}
\label{sec:ch4-zigbee-layer}

Tầng thiết bị Zigbee là lớp trực tiếp tương tác với môi trường vật lý. Đây là thành phần quyết định tính ổn định của toàn hệ thống vì các thiết bị phần cứng thường hoạt động liên tục trong thời gian dài và chịu ảnh hưởng lớn từ nhiễu sóng vô tuyến. Trong đồ án này, các thiết bị được thiết kế và triển khai dựa trên các kit phát triển phần cứng của hãng Silicon Labs, sử dụng công cụ cấu hình đồ họa và thư viện EmberZNet của Gecko SDK.

\subsection{Triển khai Coordinator / NCP Board}
\label{sec:ch4-ncp-firmware}

Coordinator đóng vai trò là gốc của mạng Zigbee, chịu trách nhiệm khởi tạo mạng vô tuyến, quản lý các khóa bảo mật và quyết định cho phép thiết bị mới tham gia mạng. Để tối ưu hóa hiệu năng, Coordinator được thiết triển khai theo kiến trúc Network Co-Processor (NCP). Firmware NCP chạy trực tiếp trên chip SoC EFR32MG12, đảm nhận việc xử lý các tầng giao thức Zigbee mức thấp (Physical, MAC, Network, Transport). 

NCP giao tiếp với Gateway Host thông qua giao tiếp UART vật lý bằng giao thức mã hóa dòng lệnh EZSP/ASH (EmberZNet Serial Protocol / Asynchronous Serial Host). Cách phân tách này giúp giải phóng tài nguyên tính toán cho Gateway Host (chạy trên Linux) và tăng độ tin cậy khi truyền nhận dữ liệu vô tuyến. Bản ghi build và phân phối firmware NCP được chỉ định tại \path|artifact/ncp-uart-hw/manifest.json|.

\subsection{Triển khai Router đèn (Z3Light Device)}
\label{sec:ch4-light-firmware}

Thiết bị đèn Z3Light được cấu hình chạy ở chế độ Zigbee Router. Thiết bị này hoạt động bằng nguồn điện lưới xoay chiều trực tiếp, tham gia định tuyến gói tin cho các thiết bị khác trong mạng mesh, đồng thời expose các cluster chức năng bao gồm:
\begin{itemize}
    \item \texttt{On/Off Cluster} (Cluster ID \texttt{0x0006}): Nhận lệnh bật/tắt và báo cáo trạng thái nguồn của đèn.
    \item \texttt{Level Control Cluster} (Cluster ID \texttt{0x0008}): Nhận lệnh thay đổi độ sáng và báo cáo chỉ số phần trăm độ sáng hiện tại (Level).
\end{itemize}
Khi trạng thái phần cứng của đèn thay đổi (ví dụ người dùng tắt bằng nút bấm phụ hoặc qua lệnh không dây), firmware đèn sẽ tự động gửi attribute report chứa giá trị \texttt{On/Off} hoặc \texttt{CurrentLevel} về NCP Coordinator. Artifact binary và manifest cấu hình cho Z3Light được quản lý tại \path|artifact/Z3Light/manifest.json|.

\subsection{Triển khai Router công tắc (Z3Switch Device)}
\label{sec:ch4-switch-firmware}

Thiết bị công tắc Z3Switch cũng được triển khai ở vai trò Zigbee Router nhằm tăng mật độ các nút định tuyến trong mạng mesh. Khác với thiết bị đèn, Z3Switch hoạt động chủ yếu như một thiết bị gửi sự kiện đầu vào. Khi người dùng nhấn nút nhấn trên kit phát triển phần cứng, Z3Switch sẽ phát lệnh ZCL gửi về Coordinator. Lệnh này được dịch thành sự kiện toggle trên Gateway. Ở các phiên bản smoke test cục bộ, Z3Switch có thể được cấu hình liên kết trực tiếp (direct binding) với Z3Light ở lớp Zigbee nhằm giảm độ trễ điều khiển vật lý. Tuy nhiên, để tích hợp vào hệ thống giám sát và quản lý tự động, sự kiện từ công tắc vẫn được gửi lên MQTT Broker để Cloud Backend xử lý. Artifact cấu hình cho Z3Switch được lưu tại \path|artifact/Z3Switch/manifest.json|.

\subsection{Triển khai cảm biến hiện diện (Motion/Occupancy Sensor)}
\label{sec:ch4-occupancy-firmware}

Cảm biến hiện diện được triển khai dưới vai trò Zigbee Router (loại thiết bị \texttt{motion}), vừa phát hiện chuyển động vừa tham gia định tuyến gói tin trong mạng mesh. Firmware của thiết bị sử dụng tệp cấu hình Zigbee Cluster Library \path|end_devices/Z3_Occupancy_Sensor/config/zcl/zcl_config.zap| và bật \texttt{Occupancy Sensing Cluster} (Cluster ID \texttt{0x0406}). Dựa trên cảm biến hồng ngoại thụ động PIR gắn trên chân GPIO, thiết bị đọc trạng thái theo chu kỳ và báo cáo giá trị \texttt{occupied} (có người) hoặc \texttt{unoccupied} (không có người) về Coordinator thông qua bản tin attribute report.

Artifact binary và manifest cấu hình của cảm biến được quản lý hoàn chỉnh tại \path|artifact/Z3_Occupancy_Sensor/manifest.json|, tương tự các thiết bị Router khác trong hệ thống. Kết quả gia nhập mạng và vận hành thực tế của cảm biến được trình bày chi tiết trong Chương 5.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\footnotesize, >=Stealth]
        \tikzset{ndc/.style={draw, circle, align=center, minimum size=1.95cm, line width=0.5pt, font=\scriptsize, inner sep=1pt}}
        \node[fblk, cGw, minimum width=2.4cm, minimum height=1.3cm] (gw) at (-4.2,0) {Gateway\\(Host Linux)};
        \node[ndc, cZig] (ncp) at (0,0) {NCP\\Coordinator};
        \node[ndc, cCloud] (light) at (4.3,1.9) {Z3Light\\Router};
        \node[ndc, cCloud] (switch) at (4.3,-1.9) {Z3Switch\\Router};
        \node[ndc, cCloud] (sensor) at (8.6,0) {Cảm biến\\chuyển động\\(Router)};
        \draw[<->, line width=0.7pt] (gw) -- node[flbl, above] {EZSP / UART} (ncp);
        \draw[<->, line width=0.6pt, dashed] (ncp) -- node[flbl, above, sloped] {Liên kết mesh} (light);
        \draw[<->, line width=0.6pt, dashed] (ncp) -- (switch);
        \draw[<->, line width=0.6pt, dashed] (ncp) -- (sensor);
        \draw[<->, line width=0.6pt, dashed] (light) -- (switch);
        \draw[<->, line width=0.6pt, dashed] (light) -- node[flbl, above, sloped] {Liên kết mesh} (sensor);
    \end{tikzpicture}
    \caption{Sơ đồ cấu trúc topo mạng Zigbee mesh được xây dựng trong đồ án.}
    \label{fig:ch4-zigbee-topology}
\end{figure}

Sơ đồ topo mạng Zigbee mesh ở Hình \ref{fig:ch4-zigbee-topology} thể hiện sự kết nối giữa các node thiết bị vật lý. Các đường nét đứt đại diện cho liên kết định tuyến tự động giữa các Router với nhau, tạo nên khả năng tự phục hồi mạng khi một node Router gặp sự cố mất điện đột ngột.

\begin{table}[H]
\centering
\caption{Thông tin minh chứng các tệp tin cấu hình và firmware thiết bị}
\label{tab:ch4-firmware-evidence}
\footnotesize
\begin{tabular}{|p{2.8cm}|p{2.8cm}|p{3.2cm}|p{4.7cm}|}
\hline
\textbf{Thiết bị} & \textbf{Vai trò mạng} & \textbf{Tệp manifest/cấu hình} & \textbf{Nội dung kỹ thuật học tập \& kiểm chứng} \\ \hline
NCP Coordinator & Coordinator & \path|artifact/ncp-uart-hw/manifest.json| & Cấu hình kênh vô tuyến, PAN ID mạng và EZSP UART driver. \\ \hline
Z3Light & Router & \path|artifact/Z3Light/manifest.json| & Cluster \texttt{On/Off} và \texttt{Level Control}, xử lý lệnh thay đổi đèn. \\ \hline
Z3Switch & Router & \path|artifact/Z3Switch/manifest.json| & Bắt sự kiện button nhấn vật lý và phát bản tin ZCL toggle. \\ \hline
Motion Sensor & Router & \path|artifact/Z3_Occupancy_Sensor/manifest.json| & Đọc cảm biến PIR, truyền bản tin thay đổi trạng thái hiện diện. \\ \hline
\end{tabular}
\end{table}

Bảng \ref{tab:ch4-firmware-evidence} tổng kết chi tiết vai trò mạng cũng như tệp cấu hình làm bằng chứng triển khai thực tế của các thiết bị.

% ========================================
% 4.4 TRIỂN KHAI GATEWAY EDGE
% ========================================
\section{Triển khai Gateway Edge}
\label{sec:ch4-gateway}

Gateway Edge đóng vai trò là "biên dịch viên" giao thức tại biên mạng. Nó chuyển đổi các bản tin JSON điều khiển có cấu trúc từ Cloud Backend thành các bản tin nhị phân ZCL thô gửi tới NCP Coordinator qua UART và ngược lại. Gateway được viết bằng ngôn ngữ native C chạy trên nền tảng Linux nhúng, giúp giảm thiểu độ trễ, tiết kiệm bộ nhớ RAM và tăng hiệu năng hoạt động. Điều này thay thế cho mô hình bridge cũ sử dụng tiến trình trung gian viết bằng Python.

\subsection{MQTT client và command queue}
\label{sec:ch4-gateway-arch}

Module \texttt{app\_mqtt.c} là cổng giao tiếp chính của Gateway với thế giới bên ngoài. Module này khởi tạo một tiến trình kết nối MQTT client sử dụng thư viện \texttt{libmosquitto}. Gateway đăng ký subscribe các topic nhận lệnh điều khiển dạng \path|commands/+/request|. Khi nhận được một bản tin MQTT command từ Broker, tiến trình callback của MQTT sẽ không xử lý trực tiếp bản tin đó nhằm tránh làm nghẽn luồng nhận tin của mạng. Thay vào đó, bản tin được chuyển sang cấu trúc JSON, parse envelope và đưa vào một hàng đợi không đồng bộ (Command Queue) dạng Ring Buffer nội bộ. Cách tiếp cận này giúp tách biệt hoàn toàn luồng truyền thông mạng và luồng thực thi lệnh của thiết bị Zigbee.

\subsection{Command handler và điều khiển thiết bị}
\label{sec:ch4-correlation-impl}

Trong vòng lặp chính của Gateway (main tick), chương trình sẽ liên tục thăm dò hàng đợi. Khi phát hiện có lệnh mới, module \texttt{cmd\_handler.c} sẽ lấy lệnh ra khỏi hàng đợi để phân tích cú pháp. Chương trình sẽ bóc tách phong bì tin MQTT (envelope), trích xuất \texttt{command\_id}, \texttt{device\_id}, tên cluster và thao tác yêu cầu.

Sau đó, \texttt{device\_dispatch.c} thực hiện ánh xạ từ \texttt{device\_id} logic thành địa chỉ EUI64 vô tuyến và mã Endpoint mạng của thiết bị Zigbee tương ứng. Cuối cùng, module \texttt{light\_ctrl.c} sẽ gọi hàm của SDK để đóng gói các bản tin ZCL tương ứng với cluster On/Off gửi xuống NCP qua giao tiếp EZSP.

Một thiết kế quan trọng trong hệ thống điều khiển là cơ chế theo dõi lệnh (correlation). Khóa chính để xác định một lệnh duy nhất là \texttt{command\_id} nằm trên topic \path|commands/{command_id}/request|. Trường \texttt{correlation\_id} được đính kèm trong payload JSON đóng vai trò là chuỗi theo vết cho cùng một luồng nghiệp vụ trên hệ thống. Gateway phải đảm bảo giữ nguyên cặp mã này khi trả lời kết quả về topic \path|commands/{command_id}/reply| để Cloud có thể phân biệt và xác nhận lệnh.

\subsection{Discovery, registry và rule cục bộ}
\label{sec:ch4-local-rules}

Gateway quản lý thông tin các thiết bị đã kết nối thông qua tệp tin \texttt{device\_registry.c}. Khi một thiết bị mới hoàn thành quá trình commissioning và join mạng thành công, module \texttt{device\_discovery.c} sẽ thu thập thông tin về EUI64, node ID, mã endpoint và danh sách cluster được hỗ trợ để lưu vào bộ nhớ cục bộ của Gateway, đồng thời publish bản tin đăng ký (registry) lên MQTT Broker để Cloud Backend cập nhật cơ sở dữ liệu.

Về mặt tự động hóa tại biên, mã nguồn Gateway có tích hợp module \texttt{rule\_engine.c} để xử lý các liên kết trực tiếp cục bộ. Tuy nhiên, tính năng này hiện tại được ghi nhận ở mức hỗ trợ các luật cứng cấu hình tĩnh (ví dụ khi có sự kiện từ công tắc Z3Switch, Gateway sẽ tự động gửi lệnh điều khiển bật/tắt tới đèn Z3Light mà không cần đi qua Cloud). Hệ thống chưa hỗ trợ cơ chế nhận, lưu trữ và đồng bộ hóa các luật động (dynamic rules) từ Cloud xuống Gateway.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\footnotesize, >=Stealth]
        \node[fblk, cInfra, minimum width=2.5cm] (broker)  at (0,0)   {Mosquitto\\Broker};
        \node[fblk, cGw,    minimum width=2.5cm] (mqtt)    at (3.3,0) {app\_mqtt.c\\(MQTT Client)};
        \node[fblk, cQueue, minimum width=2.5cm] (queue)   at (6.6,0) {Hàng đợi lệnh\\(Ring Buffer)};
        \node[fblk, cGw,    minimum width=2.5cm] (handler) at (9.9,0) {cmd\_handler.c\\(Bộ phân tích)};
        \node[fblk, cGw,    minimum width=2.5cm] (dispatch) at (9.9,-2.0) {device\_dispatch.c\\(Bộ điều phối)};
        \node[fblk, cGw, minimum width=2.5cm] (ctrl) at (6.6,-4.0) {light\_ctrl.c\\(Điều khiển đèn)};
        \node[fblk, cGw, minimum width=2.5cm] (reg)  at (9.9,-4.0) {device\_registry.c\\(Registry cục bộ)};
        \node[fblk, cGw, minimum width=2.5cm] (rule) at (13.2,-4.0) {rule\_engine.c\\(Luật cục bộ)};
        \draw[fbi]  (broker) -- (mqtt);
        \draw[farr] (mqtt) -- (queue);
        \draw[farr] (queue) -- (handler);
        \node[flbl] at (1.65,0.72) {MQTT};
        \node[flbl] at (4.95,0.72) {Nạp lệnh};
        \node[flbl] at (8.25,0.72) {Lấy lệnh};
        \draw[farr] (handler) -- node[flbl, right] {Điều phối} (dispatch);
        \draw[farr] (dispatch.south) -- ++(0,-0.6) -| (ctrl.north);
        \draw[farr] (dispatch.south) -- ++(0,-0.6) -| (reg.north);
        \draw[farr] (dispatch.south) -- ++(0,-0.6) -| (rule.north);
    \end{tikzpicture}
    \caption{Kiến trúc luồng xử lý không đồng bộ bên trong phần mềm Gateway Edge.}
    \label{fig:ch4-gateway-architecture}
\end{figure}

Kiến trúc phân rã các module xử lý của Gateway ở Hình \ref{fig:ch4-gateway-architecture} thể hiện tính độc lập và khả năng xử lý song song của Gateway. Khi MQTT nhận lệnh mới, luồng chính không bị chặn mà lệnh được chuyển vào Ring Buffer chờ luồng logic tiêu thụ theo thứ tự thời gian.

\begin{table}[H]
\centering
\caption{Bản đồ phân rã chức năng các file mã nguồn Gateway C}
\label{tab:ch4-gateway-modules}
\footnotesize
\begin{tabular}{|p{3.2cm}|p{3.5cm}|p{7.8cm}|}
\hline
\textbf{Module} & \textbf{Tệp tin mã nguồn} & \textbf{Vai trò và nhiệm vụ cụ thể} \\ \hline
MQTT Client & \path|app_mqtt.c| & Khởi tạo thư viện \texttt{libmosquitto}, duy trì kết nối MQTT, nhận lệnh điều khiển và đưa vào hàng đợi. \\ \hline
Command Handler & \path|cmd_handler.c| & Giải mã phong bì JSON MQTT, phân tích cú pháp các lệnh điều khiển thuộc envelope \texttt{sb.v1}. \\ \hline
Device Dispatcher & \path|device_dispatch.c| & Phân phối các lệnh đã phân tích tới module phần cứng tương ứng, chuyển đổi định danh logic thành thông tin mạng Zigbee. \\ \hline
Light Control & \path|light_ctrl.c| & Đóng gói bản tin ZCL On/Off và Level gửi xuống NCP để điều khiển thiết bị đèn vật lý. \\ \hline
Device Registry & \path|device_registry.c| & Quản lý cơ sở dữ liệu lưu tạm của các thiết bị trong mạng vô tuyến (EUI64, Node ID, Endpoints). \\ \hline
Rule Engine & \path|rule_engine.c| & Thực thi các liên kết cục bộ (local rules) giữa switch và light khi cấu hình môi trường được kích hoạt. \\ \hline
\end{tabular}
\end{table}

Bảng \ref{tab:ch4-gateway-modules} thể hiện rõ sự phân công trách nhiệm của từng file mã nguồn trong Gateway Edge, giúp hệ thống hoạt động ổn định và dễ bảo trì.

% ========================================
% 4.5 TRIỂN KHAI CLOUD BACKEND
% ========================================
\section{Triển khai Cloud Backend}
\label{sec:ch4-cloud}

Cloud Backend đóng vai trò là bộ não điều phối tập trung của hệ thống, quản lý dữ liệu, cung cấp giao diện lập trình cho ứng dụng di động và thực hiện các chức năng tự động hóa phức tạp. Lớp ứng dụng này được xây dựng bằng ngôn ngữ Python, sử dụng framework \texttt{FastAPI} bất đồng bộ (async), kết hợp với thư viện ORM \texttt{SQLAlchemy} để làm việc với hệ quản trị cơ sở dữ liệu quan hệ PostgreSQL.

\subsection{FastAPI app lifecycle và router}
\label{sec:ch4-cloud-lifecycle}

Tệp tin \path|cloud/app/main.py| là điểm khởi động của ứng dụng Cloud Backend. Trong hàm sự kiện vòng đời khởi tạo (\texttt{lifespan}), ứng dụng sẽ thiết lập kết nối cơ sở dữ liệu, khởi tạo dịch vụ MQTT client và kích hoạt tiến trình chạy nền xử lý sự kiện timeout của lệnh điều khiển. Các nhóm API được chia nhỏ và quản lý trong các file router riêng biệt trong thư mục \path|routers/|:
\begin{itemize}
    \item \path|routers/devices.py|: Cung cấp API đọc thông tin danh sách và chi tiết thiết bị cùng các trạng thái được báo cáo tương ứng.
    \item \path|routers/commands.py|: Tiếp nhận các yêu cầu điều khiển từ Mobile App, khởi tạo tiến trình ghi nhận lệnh và truyền tin không đồng bộ.
    \item \path|routers/events.py|: Cung cấp lịch sử sự kiện của các thiết bị phục vụ chức năng hiển thị log trên ứng dụng.
    \item \path|routers/automations.py|: Quản lý các thao tác CRUD (tạo, đọc, sửa, xóa) đối với các luật tự động hóa trên Cloud.
    \item \path|routers/gateways.py| và \path|routers/provisioning.py|: Điều khiển các tính năng mức hệ thống của Gateway như mở/đóng permit join hoặc kích hoạt quét lại thiết bị.
\end{itemize}

\subsection{Database model và command lifecycle}
\label{sec:ch4-cloud-models}

Các mô hình dữ liệu chính được định nghĩa bằng ORM trong file \path|cloud/app/models.py|. Hệ thống bao gồm các bảng quan hệ cơ bản như \texttt{Device}, \texttt{DeviceState}, \texttt{Event}, \texttt{Command}, \texttt{Automation} và \texttt{ProvisioningSession}. 

Một thiết kế quan trọng cần nhấn mạnh là vòng đời của lệnh điều khiển (\texttt{Command}). Lệnh được khởi tạo trên DB với trạng thái ban đầu là \texttt{accepted}, sau đó Cloud publish thông điệp MQTT và chuyển sang trạng thái \texttt{queued}. Khi nhận được phản hồi ACK từ Gateway qua topic reply, Cloud cập nhật trạng thái lệnh tương ứng thành \texttt{executed} hoặc \texttt{failed}. Nếu lệnh không nhận được phản hồi sau một khoảng thời gian cấu hình sẵn, tiến trình chạy nền \path|cloud/app/command_timeout.py| sẽ tự động quét và cập nhật trạng thái lệnh thành \texttt{timeout}.

\subsection{Cấu hình biến môi trường}
\label{sec:ch4-env}

Mọi tham số cấu hình nhạy cảm của Cloud Backend được tách biệt khỏi mã nguồn và quản lý thông qua thư viện \texttt{Pydantic Settings} bằng các biến môi trường có tiền tố \texttt{SB\_}. Việc phân tách này bảo vệ hệ thống khỏi nguy cơ rò rỉ thông tin đăng nhập trên các hệ thống quản lý mã nguồn (như GitHub). Bảng \ref{tab:ch4-env} thể hiện các cấu hình chính.

\begin{table}[H]
\centering
\caption{Danh mục biến môi trường của dịch vụ Cloud Backend}
\label{tab:ch4-env}
\footnotesize
\begin{tabular}{|p{4.2cm}|p{9.8cm}|}
\hline
\textbf{Biến môi trường} & \textbf{Vai trò và ý nghĩa cấu hình} \\ \hline
\texttt{SB\_DATABASE\_URL} & Chuỗi kết nối bất đồng bộ tới database PostgreSQL (chứa credentials, địa chỉ máy chủ và tên DB). \\ \hline
\texttt{SB\_MQTT\_HOST}, \texttt{SB\_MQTT\_PORT} & Địa chỉ IP/Domain và cổng kết nối vô tuyến của MQTT Mosquitto Broker. \\ \hline
\texttt{SB\_MQTT\_USERNAME}, \texttt{SB\_MQTT\_PASSWORD} & Thông tin tài khoản xác thực kết nối MQTT để phân quyền truy cập. \\ \hline
\texttt{SB\_COMMAND\_TIMEOUT\_MS} & Ngưỡng thời gian tối đa (mili-giây) Cloud chờ phản hồi của Gateway trước khi đánh dấu timeout cho lệnh. \\ \hline
\texttt{SB\_TENANT\_ID}, \texttt{SB\_SITE\_ID} & Thông tin namespace định danh phục vụ phân chia tài nguyên và xây dựng cấu trúc cây topic. \\ \hline
\end{tabular}
\end{table}

Bảng~\ref{tab:ch4-env} liệt kê các biến môi trường bắt buộc cần cấu hình trước khi khởi động dịch vụ Cloud Backend trên môi trường production. Việc tách biệt cấu hình nhạy cảm ra khỏi mã nguồn thông qua các biến môi trường là thực hành bảo mật tiêu chuẩn, đảm bảo thông tin đăng nhập không bị lộ qua hệ thống quản lý phiên bản mã nguồn.

\subsection{MQTT service và timeout worker}
\label{sec:ch4-sync-impl}

Dịch vụ \path|cloud/app/mqtt_client.py| chạy song song dưới dạng một tiến trình bất đồng bộ trong FastAPI. Nó subscribe các topic thông báo từ Gateway bao gồm reported state, events, registry và gateway health. Khi nhận được bản tin báo cáo trạng thái mới từ một thiết bị, module này sẽ thực hiện hàm upsert (cập nhật nếu có hoặc chèn mới) trạng thái vào bảng \texttt{device\_states}. 

Tiến trình timeout worker chạy độc lập định kỳ (mỗi giây một lần) để quét các bản ghi lệnh điều khiển có thời gian tạo vượt quá thời gian timeout quy định mà vẫn đang ở trạng thái trung gian, đảm bảo tính chính xác của dữ liệu hệ thống.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\footnotesize, >=Stealth]
        \node[fblk, cCloud, minimum width=3.4cm] (acc)  at (0,0)    {ACCEPTED\\(Tạo trong CSDL)};
        \node[fblk, cCloud, minimum width=3.4cm] (que)  at (0,-1.9) {QUEUED\\(Đã gửi lên MQTT)};
        \node[fblk, cCloud, minimum width=3.4cm] (sent) at (0,-3.8) {SENT\\(Gateway đã ACK)};
        \node[fblk, cCloud, minimum width=3.4cm] (exec) at (0,-5.7) {EXECUTED\\(GW đã gửi ZCL)};
        \node[fblk, cWarn, minimum width=3.4cm] (tmo)  at (5.5,-1.9) {TIMEOUT\\(Hết hạn trên CSDL)};
        \node[fblk, cWarn, minimum width=3.4cm] (fail) at (5.5,-3.8) {FAILED\\(Có lỗi xảy ra)};
        \draw[farr] (acc)  -- (que);
        \draw[farr] (que)  -- (sent);
        \draw[farr] (sent) -- (exec);
        \draw[farr] (que)  -- node[flbl, above] {hết hạn} (tmo);
        \draw[farr] (sent) -- node[flbl, above] {lỗi} (fail);
        \draw[farr] (acc.east) -- node[flbl, above, pos=0.12] {lỗi} ++(7.6,0) |- (fail.east);
    \end{tikzpicture}
    \caption{Lược đồ chuyển trạng thái trong vòng đời lệnh điều khiển trên Cloud Backend.}
    \label{fig:ch4-cloud-lifecycle}
\end{figure}

Lược đồ vòng đời lệnh điều khiển trên Cloud Backend (Hình \ref{fig:ch4-cloud-lifecycle}) chứng minh tính chặt chẽ trong thiết kế trạng thái lệnh. Nhờ các trạng thái trung gian, Mobile App có thể biết chính xác lệnh của mình đang bị nghẽn ở Cloud, ở đường truyền MQTT, hay tại Gateway.

\begin{table}[H]
\centering
\caption{Bản đồ phân rã chức năng các file mã nguồn Cloud Backend}
\label{tab:ch4-cloud-modules}
\footnotesize
\begin{tabular}{|p{3.2cm}|p{3.5cm}|p{7.8cm}|}
\hline
\textbf{Module} & \textbf{Tệp tin mã nguồn} & \textbf{Vai trò và nhiệm vụ cụ thể} \\ \hline
FastAPI Lifecycle & \path|cloud/app/main.py| & Điểm khởi chạy ứng dụng, khai báo các routers, quản lý vòng đời kết nối DB và Broker. \\ \hline
Database Models & \path|cloud/app/models.py| & Định nghĩa các bảng dữ liệu bằng SQLAlchemy ORM quan hệ: devices, commands, automations, v.v. \\ \hline
MQTT Client Service & \path|cloud/app/mqtt_client.py| & Lắng nghe tin báo cáo từ Gateway để lưu trữ trạng thái thiết bị và cập nhật kết quả thực hiện lệnh. \\ \hline
Timeout Worker & \path|cloud/app/command_timeout.py| & Tiến trình quét nền định kỳ phát hiện và xử lý các bản ghi lệnh bị hết hạn phản hồi từ Gateway. \\ \hline
API Routers & \path|cloud/app/routers/*.py| & Định nghĩa các endpoint REST phục vụ giao dịch dữ liệu với ứng dụng di động Flutter. \\ \hline
\end{tabular}
\end{table}

Bảng \ref{tab:ch4-cloud-modules} tóm tắt các tệp tin cấu thành dịch vụ Cloud Backend, phân định rõ các tầng xử lý từ API REST tới kết nối Broker và cơ sở dữ liệu.

% ========================================
% 4.6 TRIỂN KHAI MOBILE APP
% ========================================
\section{Triển khai Mobile App}
\label{sec:ch4-mobile}

Ứng dụng di động (Mobile App) là giao diện tương tác trực tiếp của người dùng với hệ thống Smart Building. Ứng dụng được xây dựng bằng công nghệ đa nền tảng Flutter (ngôn ngữ Dart), tổ chức mã nguồn theo mô hình phân lớp rõ ràng nhằm tách biệt logic nghiệp vụ khỏi tầng hiển thị giao diện người dùng (UI).

Một nguyên tắc kiến trúc cực kỳ quan trọng cần khẳng định là: \textbf{Mobile App không kết nối trực tiếp đến MQTT Broker}. Mọi yêu cầu điều khiển hay truy vấn dữ liệu của người dùng đều đi qua cổng bảo mật REST API của Cloud Backend. Lựa chọn thiết kế này mang tính chất định hướng sản phẩm (product-facing) vì nó giúp đơn giản hóa việc quản lý trạng thái kết nối trên thiết bị di động (tránh hao pin do duy trì socket MQTT liên tục), đồng thời tập trung hóa việc kiểm tra quyền truy cập và lưu lịch sử bảo mật tại máy chủ Cloud.

Mã nguồn ứng dụng di động được phân rã thành các lớp chính:
\begin{itemize}
    \item \textbf{Lớp API Client:} Nằm tại tệp \path|mobile_app/lib/data/services/api_client.dart|, phụ trách thiết lập kết nối HTTP Client, đính kèm cấu hình header và gửi nhận các payload JSON REST API với Cloud Backend.
    \item \textbf{Lớp Data Repositories:} Nằm trong thư mục \path|mobile_app/lib/data/repositories/| (ví dụ \path|remote_device_repository.dart|). Lớp này nhận dữ liệu thô từ API Client, chuyển đổi thành các thực thể dữ liệu (entities) chuẩn của ứng dụng và cung cấp cho tầng giao diện.
    \item \textbf{Lớp Domain Models:} Nằm trong thư mục \path|mobile_app/lib/domain/|, định nghĩa cấu trúc dữ liệu của các thực thể trong ứng dụng như \texttt{Device}, \texttt{Command}, \texttt{AutomationRule} để đảm bảo tính nhất quán của dữ liệu.
    \item \textbf{Lớp UI Features:} Nằm tại thư mục \path|mobile_app/lib/ui/features/|, bao gồm các màn hình chức năng như danh sách thiết bị (\path|devices_view.dart|), chi tiết điều khiển (\path|device_detail_view.dart|), quản lý luật tự động hóa (\path|automation_rules_view.dart|) và theo dõi lịch sử hệ thống (\path|logs_view.dart|).
\end{itemize}

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\footnotesize, >=Stealth]
        \node[fblk, cMob,   minimum width=5.2cm] (ui)     at (0,0)    {\textbf{UI Features (Views)}\\devices, detail, rules, logs};
        \node[fblk, cCloud, minimum width=5.2cm] (vm)     at (0,-1.6) {\textbf{View Models (Trạng thái)}\\Provider / ChangeNotifier};
        \node[fblk, cQueue, minimum width=5.2cm] (repo)   at (0,-3.2) {\textbf{Repositories}\\remote\_device\_repository.dart};
        \node[fblk, cGw,    minimum width=5.2cm] (client) at (0,-4.8) {\textbf{API Client (HTTP)}\\api\_client.dart};
        \node[fblk, cInfra, minimum width=5.2cm] (cl)     at (0,-6.4) {\textbf{Cloud Backend REST API}};
        \draw[farr] (ui) -- (vm);
        \draw[farr] (vm) -- (repo);
        \draw[farr] (repo) -- (client);
        \draw[farr] (client) -- node[flbl, right] {HTTPS GET/POST} (cl);
    \end{tikzpicture}
    \caption{Kiến trúc phân lớp cấu trúc mã nguồn bên trong ứng dụng di động Flutter.}
    \label{fig:ch4-mobile-layers}
\end{figure}

Sơ đồ phân lớp của Mobile App ở Hình \ref{fig:ch4-mobile-layers} chỉ ra luồng giao tiếp một chiều từ trên xuống dưới. Nhờ cấu trúc này, khi cần thay đổi giao diện hiển thị, nhà phát triển chỉ cần chỉnh sửa lớp UI mà không làm ảnh hưởng đến logic truyền nhận dữ liệu ở lớp API Client phía dưới.

\begin{table}[H]
\centering
\caption{Bản đồ cấu trúc thư mục mã nguồn và vai trò của Mobile App}
\label{tab:ch4-mobile-map}
\footnotesize
\begin{tabular}{|p{3.2cm}|p{4.2cm}|p{7.1cm}|}
\hline
\textbf{Lớp cấu trúc} & \textbf{Đường dẫn thư mục / file} & \textbf{Vai trò và nhiệm vụ cụ thể} \\ \hline
API Client & \path|lib/data/services/api_client.dart| & Gửi nhận các gói tin HTTPS REST API bất đồng bộ với Cloud Backend. \\ \hline
Repositories & \path|lib/data/repositories/*| & Ánh xạ API response JSON thành các thực thể dữ liệu Dart có kiểu dữ liệu tường minh. \\ \hline
Domain Models & \path|lib/domain/*| & Khai báo mô hình dữ liệu chính thức cho các đối tượng thiết bị, sự kiện, lệnh và phòng. \\ \hline
UI Features & \path|lib/ui/features/*| & Thiết kế giao diện hiển thị danh sách thiết bị, điều khiển đèn, quản trị rules và xem log. \\ \hline
\end{tabular}
\end{table}

Bảng \ref{tab:ch4-mobile-map} cung cấp cái nhìn chi tiết về cách phân bổ các file mã nguồn của ứng dụng Mobile, củng cố tính chặt chẽ trong triển khai phần mềm phía máy khách.

\subsection{Ảnh giao diện sản phẩm theo từng chức năng}
\label{subsec:ch4-mobile-screens}

Các ảnh chụp màn hình trong mục này thể hiện lớp giao diện sản phẩm của Mobile App sau khi được triển khai. Mỗi nhóm ảnh được đặt theo chức năng người dùng nhìn thấy trực tiếp, tương ứng với các thư mục UI Feature trong mã nguồn Flutter.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.44\textwidth}
        \centering
        \includegraphics[width=0.82\linewidth]{Images/login_screen_crop_top.jpg}\\
        \footnotesize (a) Màn hình đăng nhập
    \end{minipage}
    \hfill
    \begin{minipage}{0.44\textwidth}
        \centering
        \includegraphics[width=0.82\linewidth]{Images/home_screen_crop_top.jpg}\\
        \footnotesize (b) Màn hình tổng quan hệ thống
    \end{minipage}
    \caption{Giao diện xác thực người dùng và màn hình tổng quan của Mobile App.}
    \label{fig:ch4-mobile-auth-home}
\end{figure}

Hình \ref{fig:ch4-mobile-auth-home} cho thấy luồng vào ứng dụng và màn hình chính. Sau khi đăng nhập, người dùng nhìn thấy trạng thái Home Hub, số lượng thiết bị, trạng thái trực tuyến, dữ liệu môi trường và các đèn có thể thao tác nhanh.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.44\textwidth}
        \centering
        \includegraphics[width=0.82\linewidth]{Images/device_list_crop_top.jpg}\\
        \footnotesize (a) Danh sách thiết bị
    \end{minipage}
    \hfill
    \begin{minipage}{0.44\textwidth}
        \centering
        \includegraphics[width=0.82\linewidth]{Images/provisioning_screen_crop_top.jpg}\\
        \footnotesize (b) Provisioning thiết bị mới
    \end{minipage}
    \caption{Giao diện quản lý thiết bị và thêm thiết bị Zigbee vào hệ thống.}
    \label{fig:ch4-mobile-devices-provisioning}
\end{figure}

Hình \ref{fig:ch4-mobile-devices-provisioning} minh họa hai thao tác vận hành thường gặp. Màn hình danh sách thiết bị hỗ trợ quan sát trạng thái, phòng đặt thiết bị và bộ lọc theo loại thiết bị. Màn hình Provisioning phục vụ quá trình tạo phiên thêm thiết bị, quét QR hoặc nhập thông tin thủ công, sau đó gán thiết bị vào phòng tương ứng.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.31\textwidth}
        \centering
        \includegraphics[width=0.92\linewidth]{Images/rule_list_crop_top.jpg}\\
        \footnotesize (a) Danh sách rule
    \end{minipage}
    \hfill
    \begin{minipage}{0.31\textwidth}
        \centering
        \includegraphics[width=0.92\linewidth]{Images/rule_create_trigger_crop_top.jpg}\\
        \footnotesize (b) Chọn trigger
    \end{minipage}
    \hfill
    \begin{minipage}{0.31\textwidth}
        \centering
        \includegraphics[width=0.92\linewidth]{Images/rule_create_schedule_crop_top.jpg}\\
        \footnotesize (c) Tạo schedule rule
    \end{minipage}
    \caption{Giao diện quản lý và tạo luật tự động hóa trong Mobile App.}
    \label{fig:ch4-mobile-automation}
\end{figure}

Hình \ref{fig:ch4-mobile-automation} mô tả phần Automation Rule. Người dùng có thể xem danh sách luật, bật hoặc tắt luật hiện có, tạo luật mới từ trigger theo thiết bị hoặc theo lịch chạy, sau đó lưu cấu hình lên Cloud Backend để đồng bộ với hệ thống.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.31\textwidth}
        \centering
        \includegraphics[width=0.92\linewidth]{Images/settings_screen_crop_top.jpg}\\
        \footnotesize (a) Cài đặt hệ thống
    \end{minipage}
    \hfill
    \begin{minipage}{0.31\textwidth}
        \centering
        \includegraphics[width=0.92\linewidth]{Images/language_mode_crop_top.jpg}\\
        \footnotesize (b) Chọn ngôn ngữ
    \end{minipage}
    \hfill
    \begin{minipage}{0.31\textwidth}
        \centering
        \includegraphics[width=0.92\linewidth]{Images/lightmode_home_screen_crop_top.jpg}\\
        \footnotesize (c) Giao diện sáng
    \end{minipage}
    \caption{Giao diện cài đặt, ngôn ngữ và chế độ hiển thị của Mobile App.}
    \label{fig:ch4-mobile-settings}
\end{figure}

Hình \ref{fig:ch4-mobile-settings} cho thấy phần cấu hình phía người dùng, bao gồm tài khoản, cấu hình kết nối Cloud, tham số polling, thời gian timeout lệnh, lựa chọn ngôn ngữ và chế độ hiển thị. Nhóm màn hình này giúp ứng dụng dễ dùng hơn trong quá trình demo, đồng thời tách các tham số vận hành khỏi mã nguồn.

% ========================================
% 4.7 TRIỂN KHAI MÔI TRƯỜNG CHẠY THỰC TẾ
% ========================================
\section{Triển khai môi trường chạy thực tế}
\label{sec:ch4-deploy-issues}

Để chứng minh tính khả thi của giải pháp trong thực tế, Cloud Backend, MQTT Broker và Database được đóng gói và triển khai trên máy chủ điện toán đám mây Amazon EC2 (chạy hệ điều hành Ubuntu 22.04 LTS). Quy trình triển khai sử dụng công nghệ container hóa thông qua công cụ Docker và Docker Compose nhằm đảm bảo tính đồng nhất giữa môi trường phát triển cục bộ và môi trường chạy thực tế.

Tệp tin \path|deploy/docker-compose.prod.yml| cấu hình ba container dịch vụ chính chạy trong cùng một mạng ảo nội bộ (Docker Network):
\begin{itemize}
    \item \texttt{sb-postgres}: Chạy PostgreSQL 16 làm cơ sở dữ liệu chính của hệ thống, chỉ expose cổng kết nối \texttt{5432} nội bộ cho Cloud API, không cho phép truy cập từ môi trường internet ngoài nhằm bảo mật dữ liệu.
    \item \texttt{sb-mosquitto}: Chạy Mosquitto MQTT Broker làm trục truyền tin, expose cổng \texttt{1883} (cho kết nối TCP của Gateway) và \texttt{9001} (cho kết nối WebSockets nếu cần mở rộng sau này).
    \item \texttt{sb-cloud-api}: Chạy ứng dụng FastAPI Cloud Backend, expose cổng \texttt{8000} ra bên ngoài để ứng dụng di động kết nối qua REST API.
\end{itemize}

Các cấu hình bảo mật được quản lý thông qua tệp tin \path|.env| nằm trên máy chủ EC2. Để đảm bảo an toàn thông tin, các thông số xác thực như mật khẩu truy cập cơ sở dữ liệu, khóa bảo mật JWT hay thông tin đăng nhập của MQTT Broker không được ghi trực tiếp vào các tệp tin cấu hình đưa lên GitHub, mà được tự động nạp từ các biến môi trường tại thời điểm khởi động container.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\footnotesize, >=Stealth]
        \node[fblk, cCloud, minimum width=3.4cm, minimum height=1.1cm] (pg)  at (0,0)   {PostgreSQL DB\\(Cổng nội bộ 5432)};
        \node[fblk, cCloud, minimum width=3.4cm, minimum height=1.1cm] (api) at (4.6,0) {FastAPI Cloud API\\(Cổng mở 8000)};
        \node[fblk, cInfra, minimum width=3.4cm, minimum height=1.1cm] (mos) at (9.2,0) {Mosquitto Broker\\(Cổng mở 1883)};
        \node[anchor=west, font=\footnotesize\bfseries] (title) at (-2.0,1.05) {Máy chủ AWS EC2 (Ubuntu Linux)};
        \begin{scope}[on background layer]
            \node[draw, rounded corners=3pt, fill=black!4, line width=0.5pt, fit=(pg)(api)(mos)(title), inner sep=5pt] (ec2) {};
        \end{scope}
        \node[flbl, anchor=north east] at (ec2.north east) {Đóng gói bằng Docker Compose};
        \node[fblk, cGw,  minimum width=3.4cm] (gw)  at (9.2,-3.4) {Gateway Edge\\(Z3GatewayHost C)};
        \node[fblk, cMob, minimum width=3.4cm] (mob) at (4.6,-3.4) {Mobile App\\(Flutter Client)};
        \draw[fbi] (api) -- (pg);
        \draw[fbi] (api) -- (mos);
        \draw[fbi]  (gw)  -- node[flbl, right] {MQTT (1883)} (mos);
        \draw[farr] (mob) -- node[flbl, left] {REST HTTPS (8000)} (api);
    \end{tikzpicture}
    \caption{Kiến trúc triển khai và luồng kết nối mạng của các thành phần trên AWS EC2.}
    \label{fig:ch4-deployment-view}
\end{figure}

Sơ đồ mạng triển khai ở Hình \ref{fig:ch4-deployment-view} chỉ rõ biên giới bảo mật của Docker Network trên máy chủ AWS EC2. Dữ liệu nhạy cảm lưu trong cơ sở dữ liệu PostgreSQL chỉ có thể được truy cập gián tiếp thông qua các endpoint API của FastAPI, bảo vệ an toàn cho thông tin của hệ thống tòa nhà thông minh.

% ========================================
% 4.8 LUỒNG VẬN HÀNH END-TO-END
% ========================================
\section{Luồng vận hành End-to-End Command}
\label{sec:ch4-e2e}

Để chứng minh sự phối hợp đồng bộ giữa các thành phần đã triển khai, luồng vận hành điều khiển thiết bị từ đầu đến cuối (End-to-End Command Flow) được thiết kế qua chuỗi 12 bước thực hiện cụ thể. Ví dụ điển hình cho luồng này là hành động người dùng thực hiện thao tác bật/tắt một thiết bị đèn từ giao diện ứng dụng di động:

\begin{enumerate}
    \item \textbf{Tương tác người dùng:} Người dùng nhấn vào biểu tượng Toggle bật/tắt đèn trên màn hình giao diện ứng dụng di động Flutter.
    \item \textbf{Yêu cầu API khách:} Lớp UI kích hoạt ViewModel, gọi API Client gửi một yêu cầu HTTP POST có dạng JSON payload chứa lệnh tới địa chỉ endpoint \path|/api/devices/{device_id}/command|.
    \item \textbf{Tiếp nhận API chủ:} Ứng dụng FastAPI trên Cloud Backend nhận yêu cầu REST, tiến hành xác thực cấu trúc dữ liệu đầu vào thông qua Pydantic schema.
    \item \textbf{Lưu vết cơ sở dữ liệu:} Cloud Backend tạo và lưu một bản ghi \texttt{Command} mới vào bảng dữ liệu PostgreSQL với trạng thái ban đầu là \texttt{accepted}.
    \item \textbf{Publish lệnh điều khiển:} Dịch vụ MQTT client trên Cloud Backend đóng gói lệnh vào một phong bì tin tiêu chuẩn \texttt{sb.v1} và publish lên MQTT topic tương ứng có dạng: \path|sb/v1/{tenant}/{site}/{gateway}/commands/{command_id}/request|.
    \item \textbf{Định tuyến Broker:} MQTT Mosquitto Broker nhận thông điệp, thực hiện định tuyến và chuyển tiếp bản tin tới các client đang subscribe.
    \item \textbf{Nhận tin tại Gateway:} Gateway Edge C nhận bản tin điều khiển từ Broker thông qua thư viện \texttt{libmosquitto} trong tệp \texttt{app\_mqtt.c}.
    \item \textbf{Xếp hàng đợi thực thi:} Gateway đưa gói tin đã nhận vào hàng đợi Command Queue nội bộ để giải phóng thread mạng.
    \item \textbf{Giải mã và Dispatch:} Vòng lặp chính của Gateway lấy lệnh khỏi hàng đợi, module \texttt{cmd\_handler.c} parse payload JSON và \texttt{device\_dispatch.c} ánh xạ sang địa chỉ mạng Zigbee tương ứng của thiết bị đích.
    \item \textbf{Truyền vô tuyến vật lý:} Module \texttt{light\_ctrl.c} đóng gói và truyền lệnh ZCL On/Off vô tuyến qua chip EFR32 NCP tới thiết bị đèn Z3Light vật lý. Đèn đổi trạng thái và phản hồi bản tin ACK.
    \item \textbf{Phản hồi trạng thái:} Gateway ghi nhận phản hồi từ thiết bị, publish bản tin xác nhận thành công lên topic reply \path|commands/{command_id}/reply|, đồng thời gửi báo cáo trạng thái mới của đèn lên topic \path|reported| tương ứng.
    \item \textbf{Cập nhật và Hiển thị:} Cloud Backend nhận bản tin reply và reported state, cập nhật bảng cơ sở dữ liệu PostgreSQL. Mobile App (thông qua cơ chế polling hoặc tải lại trang) nhận được trạng thái mới và hiển thị biểu tượng đèn đã bật lên màn hình.
\end{enumerate}

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\footnotesize, >=Stealth]
        \tikzset{stp/.style={fblk, minimum width=2.5cm, minimum height=1.0cm}}
        \node[stp, cMob]   (s1) at (0,0)    {1. Mobile App\\Bật/tắt đèn};
        \node[stp, cCloud] (s2) at (3.1,0)  {2. Cloud API\\HTTP POST};
        \node[stp, cCloud] (s3) at (6.2,0)  {3. CSDL\\Lưu lệnh};
        \node[stp, cInfra] (s4) at (9.3,0)  {4. MQTT Broker\\Định tuyến};
        \node[stp, cGw]    (s5) at (12.4,0) {5. Gateway\\app\_mqtt.c};
        \node[stp, cGw]    (s6)  at (12.4,-2.3) {6. Gateway\\cmd\_handler.c};
        \node[stp, cGw]    (s7)  at (9.3,-2.3)  {7. Gateway\\device\_dispatch.c};
        \node[stp, cGw]    (s8)  at (6.2,-2.3)  {8. Gateway\\light\_ctrl.c};
        \node[stp, cWarn]  (s9)  at (3.1,-2.3)  {9. Zigbee Light\\Thực thi};
        \node[stp, cGw]    (s10) at (0,-2.3)    {10. Gateway\\Phản hồi MQTT};
        \node[stp, cCloud] (s11) at (0,-4.6)    {11. Cloud API\\Cập nhật CSDL};
        \node[stp, cMob]   (s12) at (3.1,-4.6)  {12. Mobile App\\Cập nhật UI};
        \draw[farr] (s1) -- (s2);
        \draw[farr] (s2) -- (s3);
        \node[flbl] at (4.65,0.72) {lưu CSDL};
        \draw[farr] (s2.south) -- ++(0,-0.6) -| (s4.south);
        \node[flbl, below] at (6.2,-1.1) {gửi MQTT};
        \draw[farr] (s4) -- (s5);
        \draw[farr] (s5) -- (s6);
        \draw[farr] (s6) -- (s7);
        \draw[farr] (s7) -- (s8);
        \draw[farr] (s8) -- (s9);
        \draw[farr] (s9) -- (s10);
        \draw[farr] (s10) -- (s11);
        \draw[farr] (s11) -- (s12);
        \draw[farr, dashed] (s12.south) -- ++(0,-0.8) -| (-1.6,0) -- (s1.west);
        \node[flbl, below] at (0.75,-5.9) {Truy vấn định kỳ};
    \end{tikzpicture}
    \caption{Trình tự luồng dữ liệu 12 bước điều khiển thiết bị đèn từ Mobile App.}
    \label{fig:ch4-e2e-command-flow}
\end{figure}

Sơ đồ luồng 12 bước ở Hình \ref{fig:ch4-e2e-command-flow} chỉ ra tính bất đồng bộ trong thiết kế hệ thống. Bằng cách lưu vết lệnh trên cơ sở dữ liệu và trao đổi qua MQTT broker, hệ thống đảm bảo lệnh luôn được kiểm soát chặt chẽ ở mọi bước chạy, tránh hiện tượng mất lệnh không rõ nguyên nhân.

% ========================================
% 4.9 GIỚI HẠN HIỆN TẠI VÀ PRODUCT
% ========================================
\section{Giới hạn hiện tại và hướng product hóa}
\label{sec:ch4-ota}

Mặc dù hệ thống đã xây dựng thành công các luồng vận hành cơ bản ổn định của một giải pháp IoT đa lớp, việc đánh giá trung thực ranh giới kỹ thuật hiện tại của phiên bản prototype là cực kỳ cần thiết để định hướng nâng cấp hệ thống thành một giải pháp cấp độ sản phẩm (product-ready) thực tế.

Một trong những giới hạn lớn nhất của phiên bản hiện tại là tính năng cập nhật firmware từ xa (OTA). Trong cấu trúc mã nguồn, dự án mới chỉ triển khai tài liệu thiết kế đặc tả hợp đồng dữ liệu tại tệp \path|docs/OTA_CAMPAIGN_CONTRACT.md| và một số cấu hình hỗ trợ OTA của hãng Silicon Labs ở phía thiết bị. Hệ thống \textbf{chưa có runtime chạy thực tế cho tính năng OTA}. 

Cụ thể, Cloud Backend chưa hiện thực các API và mô hình quản lý chiến dịch cập nhật (\path|/api/ota/*|); Gateway Edge C chưa có module tải tệp tin firmware từ xa qua giao thức HTTP, chưa xử lý việc xác thực mã băm SHA256 hay kích thước tệp tin, chưa đưa tệp tin firmware vào thư mục lưu trữ cục bộ để đẩy xuống thiết bị thông qua Zigbee OTA Cluster, và chưa báo cáo tiến độ cập nhật về Cloud. Do đó, tính năng này được xác định là hướng phát triển trong tương lai.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\footnotesize, >=Stealth]
        \node[fblk, cMob, minimum width=3.7cm, minimum height=2.6cm] (c1) at (0,0) {\textbf{Đã triển khai}\\\textit{(Lõi prototype)}\\NCP/Light/Switch\\Gateway MQTT Native\\Lệnh Cloud \& CSDL\\Mobile App \& API\\Triển khai EC2 Compose};
        \node[fblk, cQueue, minimum width=3.7cm, minimum height=2.6cm] (c2) at (5.0,0) {\textbf{Mức Contract}\\\textit{(Ranh giới hệ thống)}\\OTA Campaign Schema\\Cấu hình Silicon Labs\\Luật cục bộ (một phần)\\Mobile Provisioning UI};
        \node[fblk, cFut, minimum width=3.7cm, minimum height=2.6cm] (c3) at (10.0,0) {\textbf{Hướng Product}\\\textit{(Hướng phát triển)}\\Tăng cường bảo mật\\OTA Runtime Engine\\Mở rộng đa Gateway\\Giám sát \& Cảnh báo\\Onboarding UX};
        \draw[farr, line width=0.9pt] (c1) -- (c2);
        \draw[farr, line width=0.9pt] (c2) -- (c3);
    \end{tikzpicture}
    \caption{Biểu đồ ranh giới năng lực kỹ thuật và lộ trình sản phẩm hóa của hệ thống.}
    \label{fig:ch4-product-readiness}
\end{figure}

Biểu đồ ranh giới năng lực hệ thống ở Hình \ref{fig:ch4-product-readiness} phân rõ ba mức độ trưởng thành của công nghệ. Trục lõi màu xanh đại diện cho phần đã hoàn thiện mã nguồn và sẵn sàng chạy thử nghiệm; trục màu vàng là ranh giới thiết kế đã cam kết; trục màu cam đại diện cho các hạng mục nghiên cứu phát triển tiếp theo.

\begin{table}[H]
\centering
\caption{Bảng so sánh hiện trạng prototype và hướng sản phẩm hóa}
\label{tab:ch4-product-boundary}
\footnotesize
\begin{tabular}{|p{2.6cm}|p{5.5cm}|p{6.4cm}|}
\hline
\textbf{Hạng mục} & \textbf{Hiện trạng phiên bản thử nghiệm} & \textbf{Giải pháp sản phẩm hóa trong tương lai} \\ \hline
Cập nhật OTA & Mới có đặc tả thiết kế và cấu hình Silicon Labs, chưa có runtime. & Triển khai module tải HTTP tại Gateway, model quản lý campaign tại Cloud, thực thi truyền tải vô tuyến. \\ \hline
Bảo mật & Cấu hình username/password cho MQTT. & Nâng cấp lên mTLS (cổng 8883), thiết lập cơ chế token JWT cho REST, mã hóa firmware artifact. \\ \hline
Tự động hóa & Luật Switch-to-Light cấu hình cứng tại Gateway. & Đồng bộ hóa luật động (dynamic rules) từ Cloud xuống Rule Engine của Gateway qua MQTT desired topic. \\ \hline
Khả năng giám sát & Log thô ghi ra tệp tin hoặc console của Docker. & Tích hợp công cụ giám sát Prometheus, hiển thị độ trễ và cảnh báo thiết bị offline lên Dashboard. \\ \hline
Khả năng mở rộng & Quản lý một Gateway đơn lẻ trong phòng thí nghiệm. & Hỗ trợ kiến trúc đa Gateway (multi-gateway), phân vùng quản lý thiết bị theo các tòa nhà (multi-tenant). \\ \hline
\end{tabular}
\end{table}

Bảng \ref{tab:ch4-product-boundary} chỉ ra chi tiết các khoảng trống công nghệ giữa phiên bản thử nghiệm hiện tại và một giải pháp IoT thương mại thực tế, thể hiện tư duy nghiêm túc và thực tế trong quá trình nghiên cứu khoa học của sinh viên.

% ========================================
% 4.10 TỔNG KẾT CHƯƠNG
% ========================================
\section{Tổng kết chương}
\label{sec:ch4-summary}

Chương 4 đã trình bày một cách hệ thống quá trình hiện thực hóa thiết kế kiến trúc từ lý thuyết thành các thành phần mã nguồn chạy thực tế của hệ thống IoT Smart Building. Thông qua việc phân rã từng lớp chức năng bao gồm firmware Zigbee cho các kit phát triển, Gateway Edge viết bằng ngôn ngữ native C, Cloud Backend FastAPI và ứng dụng di động Flutter, đồ án đã chứng minh được khả năng làm chủ công nghệ và xây dựng giải pháp IoT hoàn chỉnh. 

Đồng thời, chương này cũng chỉ ra một cách trung thực các giới hạn vận hành của hệ thống, đặc biệt là tính năng OTA mới dừng lại ở mức hợp đồng dữ liệu. Đây là tiền đề quan trọng để phân định rõ phạm vi kiểm thử và đánh giá hiệu năng hệ thống sẽ được trình bày chi tiết trong Chương 5 tiếp theo.
